# PixelHush Homebrew Tap

```bash
brew install --cask pixelhush/pixelhush/pixelhush
```

Or tap first:

```bash
brew tap pixelhush/pixelhush
brew install --cask pixelhush
```

[pixelhush.dev](https://pixelhush.dev)
